package com.icloud;

import java.io.IOException;
 


import org.apache.http.HttpEntity;
import org.apache.http.ParseException;
import org.apache.http.client.ClientProtocolException;
import org.apache.http.client.methods.CloseableHttpResponse;
import org.apache.http.client.methods.HttpGet;
import org.apache.http.impl.client.CloseableHttpClient;
import org.apache.http.impl.client.HttpClients;
import org.apache.http.util.EntityUtils;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.boot.test.SpringApplicationConfiguration;
import org.springframework.boot.test.TestRestTemplate;
import org.springframework.boot.test.WebIntegrationTest;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;
import org.springframework.web.client.RestTemplate;
import org.springframework.web.context.WebApplicationContext;

//import com.icloud.dao.CloudEventMapper;
//import com.icloud.service.MerchantsMpService;

/**
 * 测试应用
 * @author luoqw
 * 2016-9-8下午1:13:03
 */
//@RunWith(SpringJUnit4ClassRunner.class)
//@SpringApplicationConfiguration(classes = RibbonApplication.class)
//@WebIntegrationTest("server.port:0")
public class ApplicationTests {
//	@Autowired
//	private WebApplicationContext context; 
//
//	@Value("${local.server.port}")
//	private int port;
//
//	private MockMvc mockMvc;
//	@Autowired
//	private RestTemplate restTemplate;//  = new TestRestTemplate();
//
//	@Autowired
//	CloudEventMapper cloudEventMapper;
//	@Autowired
//	MerchantsMpService merchantsMpService;
//	
//	
//	@Before
//	public void setupMockMvc() {
//		mockMvc = MockMvcBuilders.webAppContextSetup(context).build();
//	}

/*	@Test
	public void accountCount() {
		System.out.println("数量：" + cloudEventMapper.selectByPrimaryKey((long) 1).getUserId());
		// assertEquals(1, accountMapper.countByAccount("user1"));
	}*/
	
//	@Test
//	public void mpDisable() {
//		merchantsMpService.mpDisable("1");
//		String result = restTemplate.getForObject("http://localhost:3101/mp/disable?mpId=1", String.class);
//		String result = new TestRestTemplate().getForObject("http://localhost:2002/mp/disable?mpId=1", String.class);
//		System.out.println(result);
//	}
/*	@Test
	 public void get() {  
	        CloseableHttpClient httpclient = HttpClients.createDefault();  
	        try {  
	            // 创建httpget.    
	            HttpGet httpget = new HttpGet("http://127.0.0.1:3101/mp/get/?id=1");  
	            System.out.println("executing request " + httpget.getURI());  
	            // 执行get请求.    
	            CloseableHttpResponse response = httpclient.execute(httpget);  
	            try {  
	                // 获取响应实体    
	                HttpEntity entity = response.getEntity();  
	                System.out.println("--------------------------------------");  
	                // 打印响应状态    
	                System.out.println(response.getStatusLine());  
	                if (entity != null) {  
	                    // 打印响应内容长度    
	                    System.out.println("Response content length: " + entity.getContentLength());  
	                    // 打印响应内容    
	                    System.out.println("Response content: " + EntityUtils.toString(entity));  
	                }  
	                System.out.println("------------------------------------");  
	            } finally {  
	                response.close();  
	            }  
	        } catch (ClientProtocolException e) {  
	            e.printStackTrace();  
	        } catch (ParseException e) {  
	            e.printStackTrace();  
	        } catch (IOException e) {  
	            e.printStackTrace();  
	        } finally {  
	            // 关闭连接,释放资源    
	            try {  
	                httpclient.close();  
	            } catch (IOException e) {  
	                e.printStackTrace();  
	            }  
	        }  
	    }  */
	
	
}
	
	
	
